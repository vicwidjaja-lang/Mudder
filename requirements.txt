# For the interactive terminal client (main.py)
# Provides readline-style input + clean concurrent output.
# Falls back to plain input() if not installed.
prompt_toolkit>=3.0.0

# For the MCP server (mud_mcp_server.py)
# Allows Claude to play the MUD via chat using MCP tools.
# Requires Python >= 3.10
mcp>=1.0.0
